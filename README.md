# OCR JSON Compare x3

Static web tool for comparing 3 OCR JSON results side by side.

## Features

- Compare 3 OCR JSON outputs in columns
- Highlight different and missing fields
- Search by JSON path or value
- Filter to show only differences

## GitHub Pages

This site is deployed as a static page from `index.html`.
